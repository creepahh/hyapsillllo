{
  "name": "image-popup-extension",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp background.js content.js hyapsiloimg.png manifest.json popup.html popup.js README.md dist/ && cd dist && zip -qr hyapsilo-notes.zip ."
  }
}